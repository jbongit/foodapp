spring:
  datasource:
    url: jdbc:mysql://localhost:3306/foodapp
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
stripe:
  apikey: ENTER_STRIPE_API_KEY
  publickey: ENTER_STRIPE_PUBLIC_KEY