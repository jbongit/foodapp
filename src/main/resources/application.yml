spring:
  datasource:
    url: jdbc:mysql://localhost:3306/foodapp
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-consumer-group 
    
stripe:
  apikey: ENTER_YOUR_SECRET_KEY
  publickey: ENTER_YOUR_PUBLIC_KEY

  